# ============================================================
# 旧识桥 api · admin/.htaccess
# ------------------------------------------------------------
# 禁止列出目录（admin 为纯静态文件，直接访问具体文件不受影响）。
# 若出现 500 或不支持 .htaccess，删除本文件即可。
# ============================================================

Options -Indexes
